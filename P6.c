#include<stdio.h>

main()

{
    int a;
    double b;
    float c;
    char d;

    printf("the size of integer %d\n",sizeof(a));
    printf("the size of double %d\n",sizeof(b));
    printf("the size of float %f\n",sizeof(c));
    printf("the size of char %c",sizeof(d));

    getch();

    return 0;

}
